<% unless error_message.blank? %>
	<%= tag.div class: "#{ defined?(css_class) ? css_class : nil } field_with_errors text-small" do %> <!-- alert alert-danger => pasado al form -->
		<%= error_message %>
	<% end %>
<% end %>